import { WaitCursorController } from "./wait-cursor.controller";

let waitCursor: ng.IComponentOptions = {
    controller: WaitCursorController,
    controllerAs: "$ctrl",
    templateUrl: "simple-controls/wait-cursor/wait-cursor.tpl.html",
    bindings: {

    }
};

export { waitCursor };